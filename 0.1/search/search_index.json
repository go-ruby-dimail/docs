{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"go-ruby-dimail","text":"<p>The pure-Go, Ruby-runtime-independent core of the Ruby <code>dimail</code> gem \u2014 a client for the Dimail API of the French government's La Suite num\u00e9rique platform, shaped so that go-embedded-ruby (rbgo) can bind it as <code>require \"dimail\"</code>.</p> <p>It is a thin, reflective adapter over the typed client in go-dimail. A <code>Session</code> exposes every one of the client's 91 operations through a single dynamic entry point, <code>Call</code>, which:</p> <ol> <li>maps a Ruby-style snake_case operation name (the API's <code>operationId</code>, e.g.    <code>get_domain</code>, <code>post_mailbox_v2</code>) to the corresponding Go method;</li> <li>coerces the arguments \u2014 Ruby Hashes become request structs, plain values become    path and query parameters, omitted trailing arguments default to nil;</li> <li>normalises the result into Ruby-shaped data: a Hash (<code>map[string]any</code>), an    Array (<code>[]any</code>), or a scalar.</li> </ol> <p>Nothing here depends on the Ruby runtime, so it is equally usable as a standalone Go library \u2014 a sibling of <code>go-ruby-regexp</code> and the rest of the go-ruby-* family.</p>"},{"location":"#install","title":"Install","text":"<pre><code>go get github.com/go-ruby-dimail/dimail\n</code></pre>"},{"location":"#at-a-glance","title":"At a glance","text":"<ul> <li>CGO-free \u2014 builds and tests identically on <code>amd64</code>, <code>arm64</code>, <code>riscv64</code>,   <code>loong64</code>, <code>ppc64le</code> and <code>s390x</code>.</li> <li>100 % test coverage, race-clean, enforced in CI.</li> <li>Backed by go-dimail (the typed,   OpenAPI-generated client).</li> </ul> <p>See Usage for the API, Examples for runnable Ruby, and Errors for the error surface.</p>"},{"location":"contributing/","title":"Contributing","text":"<p>go-ruby-dimail is BSD-3-Clause. The code lives at github.com/go-ruby-dimail/dimail.</p>"},{"location":"contributing/#ground-rules","title":"Ground rules","text":"<ul> <li>CGO-free. No cgo, ever \u2014 the package must build and test on all six   supported 64-bit targets (<code>amd64</code>, <code>arm64</code>, <code>riscv64</code>, <code>loong64</code>, <code>ppc64le</code>,   <code>s390x</code>).</li> <li>100 % coverage. The CI gate enforces 100 % statement coverage, including   error branches.</li> <li>Backed by go-dimail. The typed transport, models and errors live in   go-dimail; this repo is only the   Ruby-idiomatic adapter. Changes to the API surface belong upstream (regenerate   go-dimail from <code>openapi.json</code> with <code>go generate ./...</code>).</li> </ul>"},{"location":"contributing/#build-test","title":"Build &amp; test","text":"<pre><code>go build ./...\ngo vet ./...\ngo test -race -coverprofile=cover.out ./...\ngo tool cover -func=cover.out | tail -1   # must read 100.0%\n</code></pre>"},{"location":"contributing/#examples","title":"Examples","text":"<p>The Ruby examples under <code>examples/</code> are verified: their operation names are checked against the real generated API by <code>TestRubyExamplesReferenceRealOperations</code>, and (for gems rbgo binds) executed under the rbgo interpreter. Keep them runnable.</p>"},{"location":"errors/","title":"Errors","text":"<p>Any non-2xx response raises an error. The Ruby error tree the rbgo binding exposes mirrors the Go <code>APIError</code> from go-dimail:</p> <pre><code>Dimail::Error      &lt; StandardError   # base\nDimail::APIError   &lt; Dimail::Error   # any non-2xx response\n</code></pre> <p><code>Dimail::APIError</code> carries:</p> Member Meaning <code>#status</code> the HTTP status code (Integer) <code>#detail</code> the parsed FastAPI <code>detail</code> (String / Array / Hash, or nil) <code>#body</code> the raw response body (String) <code>#not_found?</code> status is 404 <code>#unauthorized?</code> status is 401 <code>#forbidden?</code> status is 403 <code>#conflict?</code> status is 409 <pre><code>begin\n  client.post_domain({ \"name\" =&gt; \"new.gouv.fr\", \"features\" =&gt; [\"mailbox\"] })\nrescue Dimail::APIError =&gt; e\n  raise unless e.conflict?\n  warn \"domain already exists\"\nend\n</code></pre>"},{"location":"errors/#in-go","title":"In Go","text":"<p><code>Session#Call</code> returns the underlying <code>*godimail.APIError</code> unchanged, so the same information is available with <code>errors.As</code>:</p> <pre><code>_, err := s.Call(ctx, \"get_domain\", \"absent.example\")\nvar apiErr *godimail.APIError\nif errors.As(err, &amp;apiErr) &amp;&amp; apiErr.NotFound() {\n    // handle 404\n}\n</code></pre>"},{"location":"examples/","title":"Examples","text":"<p>The repository ships runnable Ruby examples under <code>examples/</code>. Each runs under rbgo once the <code>require \"dimail\"</code> binding is installed; the operation names they use are checked against the real generated API by a Go test, so they cannot drift.</p>"},{"location":"examples/#basic-usage","title":"Basic usage","text":"<pre><code>require \"dimail\"\n\nclient = Dimail::Client.new(basic_auth: [\"apiuser\", \"apipass\"])\nclient.login\n\n# A single resource comes back as a Hash.\ndomain = client.get_domain(\"example.gouv.fr\")\nputs \"#{domain[\"name\"]}: #{domain[\"state\"]}\"\n\n# A collection comes back as an Array of Hashes.\nclient.get_domains.each { |d| puts d[\"name\"] }\n\n# Self-service views for the authenticated user.\nclient.get_my_domains.each { |d| puts d[\"name\"] }\n</code></pre>"},{"location":"examples/#mailboxes-and-forwards","title":"Mailboxes and forwards","text":"<pre><code>created = client.post_mailbox_v2(\"example.gouv.fr\", \"jean.dupont\",\n                                 { \"features\" =&gt; [\"ox\"] })\nputs \"created #{created[\"email\"]}\"\n\nclient.get_mailboxes_v2(\"example.gouv.fr\").each { |mb| puts mb[\"email\"] }\n\nclient.post_forward(\"example.gouv.fr\", \"contact\",\n                    { \"nexthop\" =&gt; \"team@other.example\" })\n</code></pre>"},{"location":"examples/#error-handling","title":"Error handling","text":"<pre><code>begin\n  client.get_domain(\"absent.example\")\nrescue Dimail::APIError =&gt; e\n  warn \"API error #{e.status}: #{e.detail}\"\n  warn \"no such domain\" if e.not_found?\nend\n</code></pre> <p>See Errors for the full error surface.</p>"},{"location":"usage/","title":"Usage","text":""},{"location":"usage/#ruby-under-rbgo","title":"Ruby (under rbgo)","text":"<p>Once rbgo ships the <code>require \"dimail\"</code> binding, the gem is used like this:</p> <pre><code>require \"dimail\"\n\n# Construction. Keyword options map to the Go client options:\n#   base_url:   -&gt; WithBaseURL      token:      -&gt; WithToken\n#   basic_auth: -&gt; WithBasicAuth    user_agent: -&gt; WithUserAgent  ([user, pass])\nclient = Dimail::Client.new(basic_auth: [\"user\", \"pass\"])\n\nclient.login                              # -&gt; Hash (the token); stores the bearer\n\n# Every API operation is a snake_case method named after its OpenAPI operationId.\n# Dispatch is dynamic (method_missing -&gt; Session#Call):\n#   * path parameters  -&gt; plain positional arguments\n#   * query parameters -&gt; trailing positional arguments (omit to leave unset)\n#   * request body     -&gt; a Hash\nclient.get_domain(\"example.gouv.fr\")                       # -&gt; Hash\nclient.get_domains                                         # -&gt; Array&lt;Hash&gt;\nclient.post_mailbox_v2(\"d.fr\", \"user\", { \"features\" =&gt; [\"ox\"] })\n</code></pre>"},{"location":"usage/#go","title":"Go","text":"<p>The same surface is available directly, without Ruby:</p> <pre><code>package main\n\nimport (\n    \"context\"\n    \"fmt\"\n\n    \"github.com/go-ruby-dimail/dimail\"\n)\n\nfunc main() {\n    ctx := context.Background()\n    s := dimail.NewSession(dimail.WithBasicAuth(\"user\", \"pass\"))\n    if _, err := s.Login(ctx); err != nil {\n        panic(err)\n    }\n\n    dom, err := s.Call(ctx, \"get_domain\", \"example.gouv.fr\") // any (map[string]any)\n    if err != nil {\n        panic(err)\n    }\n    fmt.Println(dom.(map[string]any)[\"state\"])\n}\n</code></pre> <p><code>Session.Client()</code> returns the underlying fully-typed <code>*godimail.Client</code>, and <code>Session.Methods()</code> lists every snake_case operation <code>Call</code> accepts.</p>"},{"location":"usage/#argument-coercion","title":"Argument coercion","text":"Parameter kind Pass as Path parameter a plain value (stringified if needed) Query parameter a value, or omit / <code>nil</code> to leave it unset Request body a Hash (JSON-round-tripped into the typed request struct) <p>Results are JSON-normalised: a single object becomes a Hash, a collection an Array of Hashes, and scalars stay scalars.</p>"}]}